#include "../functions.h"

namespace params{

std::vector<bool> flags(4, 0);//	[T, l, s, E]



uint8_t settings = 3; //settings: b0 - randomize; b1 - group terms; b2 - repeat wrongs;
std::string language = "en";
bool e = flags[3];
}

